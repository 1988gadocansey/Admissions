import { ProgrammePipePipe } from './programme-pipe.pipe';

describe('ProgrammePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProgrammePipePipe();
    expect(pipe).toBeTruthy();
  });
});
